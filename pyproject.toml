[project]
name = "mobile-agent-os"
version = "0.1.0"
description = "Stage 1 single-display asynchronous runtime for mobile AppAgents"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
