# Benchmarks

This directory contains experiment assets, not Mobile AgentOS runtime code.

- `tasks/`: curated benchmark task suites and hidden success criteria.
- `fixtures/`: controlled apps and setup assets used to make specific benchmark cases reproducible.

Runtime app profiles live in `../config/apps.json`. Core runtime code lives in `../mobile_agent_os/`.
